Creating a new timeframe

#Create MySQL tables
~/fx/data/src/createTables.pl --timeframe=7200,14400 | mysql -u root fx
vi ~/fx/fx.yml.all /etc/fx.yml

#Populate by deriving data from existing timeframes
~/fx/updateTf.pl --verbose --timeframe=7200 --available-timeframe=hour
~/fx/updateTf.pl --verbose --timeframe=14400 --available-timeframe=2hour


#Make sure the update script works with the new timeframes
~/fx/updateTf.pl --start="4 days ago at midnight" --verbose

echo issue with updateTf.pl assumes tfs are sequential and can be derived from the previous one (eg: doesn't work with 2hour to 3hour tfs)
