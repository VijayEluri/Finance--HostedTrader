Eclipse plugins
	http://download.eclipse.org/egit/updates
	http://e-p-i-c.sf.net/updates/testing
Perl modules
	Parse::RecDescent
    Inline::Java
	Config::Any
	Hash::Merge
	Test::Exception
	Data::Compare
	YAML::Tiny
    YAML::XS
	List::Compare::Functional
	MIME::Lite
	Text::ASCIITable
	Devel::Cover
	Date::Manip
	Date::Calc
	Devel::NYTProf
        Pod::Coverage
    Test::MockModule
HostedTrader
    Setup TRADER_HOME to point to source code, eg: TRADER_HOME=~/src/Hosted--Trader
    source $TRADER_HOME/setupEnv.source
	Need a config file, eg: /etc/fx.yml
    Need a FXCM config file, $TRADER_HOME/data/fxcm/servers/config_real.sh
MySQL
    to generate db/user/tables corresponding to config file, run:
      $TRADER_HOME/createDBSchema.pl | mysql -u root
	If using partitioned tables (eg, minutes timeframe tables can be very large and it makes sense to partition them)
	may need to set this in my.cnf (mysqld section)
	open-files-limit=10000
